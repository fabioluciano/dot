# Antidote pre-bundle: carregado ANTES do compinit.
# Apenas plugins que adicionam diretorios de completion ao fpath (kind:fpath),
# para que o compinit os enxergue. Nada aqui deve chamar `compdef`.

zsh-users/zsh-completions kind:fpath path:src
